import "./style.css";

/**
 * A function to present a greeting to the user
 * @param {string} message - The message to show
 */
const greeting = (message: string) => {
  alert(message);
};

greeting("Hello world");
